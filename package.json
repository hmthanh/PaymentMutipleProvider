{
  "name": "payment-multiple-provider",
  "version": "1.0.0",
  "description": "Payment system on Cloudflare Workers with multiple provider support",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "format": "prettier --write .",
    "lint": "eslint src"
  },
  "keywords": [
    "payment",
    "cloudflare",
    "workers",
    "paddle",
    "paypal",
    "edge"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.1.0",
    "@cloudflare/workers-types": "^4.20231218.0",
    "eslint": "^8.56.0",
    "prettier": "^3.1.1",
    "vitest": "^1.1.0",
    "wrangler": "^3.22.0"
  },
  "dependencies": {}
}
